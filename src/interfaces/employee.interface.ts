export interface IEmployee {
  firstName: string;
  latName: string;
  email: string;
  role: string;
  phoneNo: string;
  dateOfBirth: string;
  street: string;
  city: string;
  state: string;
}

export interface DataStoredInToken {
  _id: string;
}
