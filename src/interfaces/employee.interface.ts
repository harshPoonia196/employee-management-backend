export interface IEmployee {
  firstName: string;
  lastName: string;
  email: string;
  role: string;
  phoneNo: string;
  dateOfBirth: string;
  street: string;
  city: string;
  state: string;
  _id: string;
}

export interface DataStoredInToken {
  _id: string;
}
